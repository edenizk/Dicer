<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:textColor">@color/fut_text_color</item>
        <item name="android:textColorSecondary">@color/fut_text_dark_color</item>
        <item name="android:colorPrimaryDark">@color/fut_colorPrimaryDark</item>
        <item name="android:editTextColor">@color/fut_text_color</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@drawable/fut_bc</item>
        <item name="android:textAllCaps">false</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="MonsterTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">@color/monster_text_color</item>
        <item name="android:textColorSecondary">@color/monster_text_dark_color</item>

        <item name="android:windowBackground">@drawable/monster_bc</item>
        <item name="android:textAllCaps">false</item>
    </style>
    <style name="ATheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:textColor">#000</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="myDialogTheme" parent="@android:style/Theme.DeviceDefault.Dialog.Alert" >
    <item name="android:textColor">@color/fut_text_color</item>
    <item name="android:typeface">monospace</item>
        
        <item name="android:windowSoftInputMode">stateUnspecified|adjustPan</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:windowFrame">@null</item>
    <item name="android:background">@color/fut_text_dark_color</item>
    </style>



</resources>
